apiversion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: argocd
spec:
    destination:
        namespace: argocd
        server: https://kubernetes.default.svc
    project: default
    source:
        path: resources/namespace.yaml
        repoURL: https://github.com/danirdd92/argo_HA.git
        targetRevision: HEAD